# Smart University Schedule Telegram Bot

Полнофункциональный Telegram бот для управления и просмотра университетского расписания.

## Возможности

### Для студентов:
- 📝 Регистрация с выбором группы и подгруппы
- 📅 Просмотр расписания на сегодня, завтра и неделю
- 👨‍🏫 Просмотр расписания преподавателей
- 👤 Управление профилем
- 🔔 Удобное меню с кнопками

### Технические особенности:
- ✅ Полностью асинхронная архитектура (async/await)
- ✅ Интеграция с REST API бэкенда
- ✅ Conversation handlers для многошаговых диалогов
- ✅ Inline клавиатуры для интерактивного выбора
- ✅ Обработка ошибок и таймаутов
- ✅ Логирование всех операций

## Установка

### 1. Установите зависимости:

```bash
cd telegram_bot
pip install -r requirements.txt
```

### 2. Создайте бота в Telegram:

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота

### 3. Настройте переменные окружения:

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
BACKEND_URL=http://localhost:3000
WEBHOOK_API_KEY=your_webhook_api_key_here
ADMIN_USER_IDS=123456789,987654321
NOTIFICATION_CHECK_INTERVAL=30
```

- `TELEGRAM_BOT_TOKEN` - токен от BotFather
- `BACKEND_URL` - URL вашего бэкенда (по умолчанию http://localhost:3000)
- `WEBHOOK_API_KEY` - API ключ для доступа к webhook endpoints бэкенда (минимум 32 символа)
- `ADMIN_USER_IDS` - ID администраторов через запятую (опционально)
- `NOTIFICATION_CHECK_INTERVAL` - интервал проверки уведомлений в секундах (по умолчанию 30)

### 4. Запустите бота:

```bash
python bot.py
```

## Использование

### Основные команды:

- `/start` - Начать работу с ботом
- `/register` - Регистрация (выбор группы и подгруппы)
- `/today` - Расписание на сегодня
- `/tomorrow` - Расписание на завтра
- `/week` - Расписание на неделю
- `/teacher` - Расписание преподавателя
- `/profile` - Просмотр и редактирование профиля
- `/help` - Справка по командам

### Процесс регистрации:

1. Отправьте `/register`
2. Выберите группу из списка
3. Выберите подгруппу (1, 2 или "Вся группа")
4. Введите ваше имя
5. Готово! Теперь можно просматривать расписание

### Просмотр расписания:

Используйте кнопки меню или команды:
- **📅 Сегодня** - расписание на текущий день
- **📅 Завтра** - расписание на следующий день
- **📆 Неделя** - расписание на всю неделю

### Расписание преподавателя:

1. Отправьте `/teacher`
2. Выберите преподавателя из списка
3. Выберите период (сегодня/завтра/неделя)
4. Получите расписание

## Архитектура

### Основные компоненты:

1. **ScheduleAPI** - Класс для работы с REST API бэкенда
   - Асинхронные HTTP запросы через aiohttp
   - Автоматическое управление сессиями
   - Обработка ошибок и таймаутов

2. **Conversation Handlers** - Многошаговые диалоги
   - Регистрация пользователя
   - Выбор преподавателя и периода

3. **Message Handlers** - Обработка команд и кнопок
   - Команды бота (/start, /help, и т.д.)
   - Reply keyboard buttons
   - Inline keyboard callbacks

4. **Data Storage** - Хранение данных пользователей
   - В памяти (user_data_store)
   - Для продакшена рекомендуется использовать базу данных

### Форматирование сообщений:

Бот использует HTML разметку для красивого отображения:
- **Жирный текст** для заголовков
- Emoji для визуальной навигации
- Структурированный вывод расписания

## Интеграция с бэкендом

Бот использует следующие API endpoints:

- `GET /api/schedule/group/:groupNumber/:period` - Расписание группы
- `GET /api/schedule/teacher/:teacherId/:period` - Расписание преподавателя
- `GET /api/schedule/groups` - Список всех групп
- `GET /api/schedule/teachers` - Список всех преподавателей

### Параметры запросов:

- `period`: `today`, `tomorrow`, `week`
- `subgroup`: `1`, `2`, `all`

## Разработка

### Добавление новых функций:

1. **Новая команда:**
```python
async def my_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.message.reply_text("Hello!")

application.add_handler(CommandHandler('mycommand', my_command))
```

2. **Новый conversation handler:**
```python
STATE1, STATE2 = range(2)

conv_handler = ConversationHandler(
    entry_points=[CommandHandler('start_conv', start_conv)],
    states={
        STATE1: [MessageHandler(filters.TEXT, state1_handler)],
        STATE2: [MessageHandler(filters.TEXT, state2_handler)],
    },
    fallbacks=[CommandHandler('cancel', cancel)],
)
```

3. **Новый inline keyboard:**
```python
keyboard = [
    [InlineKeyboardButton("Option 1", callback_data="opt1")],
    [InlineKeyboardButton("Option 2", callback_data="opt2")],
]
reply_markup = InlineKeyboardMarkup(keyboard)
await update.message.reply_text("Choose:", reply_markup=reply_markup)
```

### Тестирование:

1. Используйте тестовый бот от BotFather
2. Проверьте все команды и сценарии
3. Убедитесь, что бэкенд доступен
4. Проверьте обработку ошибок

## Продакшен

### Рекомендации для продакшена:

1. **База данных** - Используйте PostgreSQL или MongoDB вместо in-memory storage
2. **Логирование** - Настройте централизованное логирование (ELK, Sentry)
3. **Мониторинг** - Добавьте метрики и алерты
4. **Масштабирование** - Используйте Redis для распределенного хранения состояний
5. **Безопасность** - Используйте переменные окружения, не храните токены в коде
6. **Webhook** - Для высоконагруженных ботов используйте webhook вместо polling

### Запуск через systemd:

Создайте файл `/etc/systemd/system/schedule-bot.service`:

```ini
[Unit]
Description=University Schedule Telegram Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/telegram_bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable schedule-bot
sudo systemctl start schedule-bot
sudo systemctl status schedule-bot
```

### Использование Docker:

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY bot.py .

CMD ["python", "bot.py"]
```

Запуск:
```bash
docker build -t schedule-bot .
docker run -d --name schedule-bot --env-file .env schedule-bot
```

## Troubleshooting

### Бот не отвечает:
1. Проверьте токен в `.env`
2. Убедитесь, что бэкенд доступен
3. Проверьте логи на ошибки

### Ошибки API:
1. Проверьте BACKEND_URL в `.env`
2. Убедитесь, что бэкенд запущен
3. Проверьте CORS настройки на бэкенде

### Проблемы с зависимостями:
```bash
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

## Лицензия

ISC

## Поддержка

Для вопросов и предложений создавайте issue в репозитории.